# Niche definitions

This package is reserved for validated declarative niche definitions: enabled engines, dependency rules, default content/forms, theme compatibility, and terminology overrides.

Niche definitions compose shared engines. This package must not contain domain entities, use cases, repositories, routers, or provider clients. No niche definitions are implemented during the architecture-only session.
