# Provider integrations

This package contains replaceable external-provider adapters. Application modules depend on provider-neutral ports; provider SDKs and webhook verification remain here. Direct provider imports from domain, application, or HTTP router code are forbidden.
